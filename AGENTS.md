# Stop the Invasion

## Product agreement
Single-player party deckbuilder, initially Bob versus infected store security in a replayable 1v1 combat laboratory. Future party of three and later two-player co-op are not this milestone. Campaign, map/shop/boss, items (potions), collectible statuses (relics), art generation, environmental rules, and menus follow combat validation.

1920x1080 design coordinates; locked 16:9 with letterboxing. Bob LEFT facing right; enemies RIGHT facing left. Comic-book horror comedy in MOREMART, a parody big-box store. Physical lit PlayCanvas cards with rounded corners, readable hover, animated pickup and action emphasis. Hand is fanned; queue is a straight, pronounced staggered stack. Mouse, keyboard and touch; no physics-driven combat.

## Rules
Six fixed internal timing positions, enemy intents anchored and exact; empty positions advance. Tiny centered dots indicate empty positions on either side of the enemy; no visible numbers, placement arrows or ghost outlines. Pickup eases the card's center to the cursor. It follows freely until release near the nearest non-enemy timing point; only then does the card settle into that position. Neighbour insertion previews remain pure; the chosen point stays visible while dragging. Inserting into an occupied player position shifts toward the nearest empty non-enemy position; ties favor later. Moving a queued card into an occupied position rotates intervening player cards, preserving gaps and enemy anchors. Moving into an empty position leaves other gaps unchanged. Invalid/full/outside drops never mutate state.

Ordinary cards automatically choose their sole valid living target when placed: damage attacks the only enemy, self-effects apply to Bob. No target dots/strips or actor-selection highlights for those obvious choices. More than one valid choice requires an explicit target; no living target rejects placement without mutation. Attachments require a card or position, not an action timing of their own. Optional explicit actor controls use click/keyboard selection without an arrow; direct dragging only drops to cards/positions. Unaffordable hand cards dim and disable before click, keyboard, touch, drag or actor-shortcut activation. Affordability uses current energy minus all commitments; zero-cost cards stay playable at zero. Refunds immediately re-enable affordable cards. Hand hover remains inspectable; native attachment-refund buttons remain usable inside disabled cards. An otherwise unaffordable card may still receive an affordable attachment. Energy bubbles below the hand distinguish available, reserved and empty capacity; generated energy cannot finance current commitments. Hands-off resolution; immediate lethal stops battle and dead actors never act. Block expires at resolution end. Exposed adds a flat bonus to the next incoming hit, then is consumed; stacks add. Draw five, start energy two, gain two subsequently, cap four; values live on actors. Unplayed cards discard unless retained; resolution-drawn cards survive into next planning. No full outcome preview. Seeded reshuffle and replay are deterministic.

Attachment cards take NO timeline position. Drop onto a compatible attack card or an empty timing point; an occupied card's exposed lower hit area targets its fixed position. Card-body targets bind exact UID and follow between hand/queue; position targets stay put, including empty positions. Only compatible living-owner damaging cards receive card attachments; position attachments affect only compatible occupants. Attachments reserve energy, can be removed/refunded before Resolve, and spend once at resolution start. Signed damage changes stack and floor each damage effect at zero before Block/Exposed. All attachments expire into discard at turn end, including terminal cleanup, empty positions and unplayed hand targets. Unused attachments still cost energy. Base card/intent effects never mutate.

## Architecture and ownership
- `src/game/types.ts`: shared types, Main owns contract changes.
- `src/game/content.ts`: data and encounter tuning, Main owns during parallel build.
- `src/game/combat.ts`: combat agent owns deterministic rules, zero DOM/PlayCanvas imports.
- `src/view/types.ts`: Main owns scene/UI interface.
- `src/view/scene.ts`: scene agent owns PlayCanvas renderer and procedural original artwork.
- `src/ui.ts`, `src/style.css`: interaction agent owns HUD, hand hit targets, drag targeting, queue editing, resolution animation orchestration.
- `src/main.ts`, `index.html`, package/config files: Main owns integration.

Combat API: `createCombat(seed?: number): CombatState`; `availableEnergy(state, actor?: ActorId): number`; `queueCard(state, uid, target: ActorId | null, slot: number): CommandResult`; `previewPlacement(state, uid, target, slot): QueueSlot[] | null`; `removeCard(state, slot)`; `moveCard(state, from, to)`; `retargetCard(state, slot, target)`; `resolveTurn(state): ResolutionStep[]`. Attachment API: `canAttachModifier(state, uid, target): boolean`; `attachModifier(state, uid, target)`; `removeModifier(state, uid)`; `damageModifier(state, uid, slot: number | null): number`. Mutating planning commands return CommandResult and mutate only on success. ModifierTarget is `{kind:'card', uid}` or `{kind:'slot', slot}`; attachments hold their source card and target. Preview is pure, shallow and accepts a hand or queued UID, preserving queued targets. Explicit position is required; modifiers cannot enter the ordinary queue. Reject invalid/dead explicit action targets, invalid/noninteger indices, wrong modifier targets and commands outside planning. Resolve preflights all commitments before spending or mutation, returns independent snapshots for six positions plus cleanup, and does not mutate its input. Attachment cards/target objects are cloned too. Generated resources cannot finance commitments. Logs are bounded; Block, discard/retention, energy/draw refresh and next-turn intent installation remain deterministic.

Renderer API: `createScene(canvas): ScenePort` in src/view/scene.ts; contract in view/types.ts. CardVisual uses top-left design x/y, width/height and rotation degrees. Cards/text/dots are textured lit meshes, not DOM replicas. Fields include target/targets, dragged, locked and damageModifier. Enemy mesh UID is EnemyAction.uid. `getCardPose(uid)` returns actual displayed bounds/rotation. Pickup, motion and release use existing position springs; displayed dimensions ease toward their targets. Reduced motion snaps instead. Hand fans at 2.3 degrees per offset from center, with a shallow vertical arc; hover and held cards straighten. Queue cards stay straight, shrink 2% and rise 7px per card in front, with the rightmost on top. Nominal queue size172x241, y500, stride132, first left544; queueCardPose aligns resting meshes and hit regions. Active actions grow to135% and rise above the stack while other cards dim; this applies to attacks, skills, energy and enemy actions. Resolution hides overlay controls/labels so enlarged art stays readable. Rounded512x768 card textures use a36px outer radius and alpha-aware materials; opacity maps refresh with modified textures. Texture keys include definition, locked state, modifier delta and target-strip visibility. Backbuffer tracks displayed bounds; DPR capped at1.5. Bob center(410,400), guard(1500,400). Hand fits above the energy bar; hover280x392 at y616 ends1008. Queue inspection is320x448 at(1540,600), within the stage. Resolve is bottom-left. Scene owns art; UI owns HUD/accessible controls.

UI API: `mountGame(root, scene): {destroy():void}`; fixed1920x1080 root scaled by Main. Same UID moves physically between hand/queue. Picked-up cards animate to cursor center, follow freely and settle at the nearest non-enemy point on release; no arrow or placement-outline rendering remains. Queue previews shift neighbours without commitment; cancellation restores dots and positions. Hand is fanned, queue is straight and staggered. Transparent timing hit regions retain keyboard focus and card-versus-position attachment selection. Modifiers brighten eligible cards and dim/reject incompatible ones. Chips identify signed effect, binding and expiry; click to remove/refund. Card chips follow UID; position chips stay put. targetChoices keeps DOM controls and physical target strips consistent: obvious ordinary targets show none. canAfford and handDisabled gate input and physical dimming without disabling valid attachment recipients or refund controls. Energy is a meter below the hand, with bright available bubbles, hatched reserved bubbles and dark empty capacity. It updates on placement, removal, attachment/refund, resolution and turn refresh. Native keyboard activation, touch cancellation, hover inspection, piles/log, outcomes/replay, reduced motion and teardown remain supported.

## Agent workflow
Main (Astra): planning, interface/content decisions, integration and final verification. Project implementation agents explicitly use openai-codex/gpt-5.6-sol. Work only assigned files. Ask Main before changing shared contracts. Escalate material product choices through Main's interactive Grill Me; use conservative defaults for implementation details. Skip builds/tests/formatters during concurrent edits; Main validates once integrated. No nested delegation without real independent ownership. Keep handoff concise: changed files, contract issues, verification not run.

## Skills and references
Read .agents/skills/build-app/SKILL.md and its direct-engine reference for PlayCanvas bootstrap; apply-conventions for engine code; scene/light/HUD/state skills as appropriate. These are official PlayCanvas skills from https://github.com/playcanvas/skills (MIT, adjacent license). Consult installed PlayCanvas declarations for exact API signatures. Interview skills from https://github.com/mattpocock/skills; use interactive ask questions when new material decisions arise. No MCP server is needed for the standalone engine; installed engine types, official docs, and browser verification are the primary tools.

## Commands
`npm install`, `npm run dev`, `npm run build`, `bun test` (behavioral regressions only). Browser-smoke actual grabs, insertion previews, locked positions, both targeting routes, card/position attachments, undo/refund/expiry, keyboard/native buttons, touch cancellation, resolve, outcomes/replay and letterboxing. No screenshot/source-text assertions. Runtime state is authoritative; animation previews never mutate it.

## Current playable and verification
Run `npm run dev`; open http://localhost:5173 or http://100.108.173.2:5173 over Tailscale. Pick up a card and release near a timing dot; the card settles there, targeting the obvious actor automatically. Occupied player timings accept insertion and enemy intent stays anchored. The hand fans; the queue stacks with pronounced depth. Hover for readable enlargement; queued cards use a large right-side preview. Watch energy bubbles beneath the hand: commitments mute bubbles and gray unaffordable cards immediately; removal restores them. Free cards remain playable at zero. Modifiers drop directly onto compatible card bodies or timing positions; chips undo/refund and all expire this turn. Cards enlarge as their actions resolve. Escape cancels, Remove returns queued cards, Replay resets.

The deck contains14 cards across9 definitions. Opening five: Percussive Maintenance, Safety-ish Vest, Trip Hazard, Duct Tape Upgrade, Break Room Coffee. Trip Hazard costs1 and reduces an enemy attack by4; Duct Tape Upgrade costs1 and adds4 to a friendly attack. Both expire this turn. Measure Once remains8 Exposed: setup plus a6-damage hit totals14 for2 energy, versus13 from Warranty Voided. Balance is experimental.

Current proof: strict TypeScript/Vite build and20 combat regressions. Chromium on the tailnet URL verified the fanned hand, straight staggered queue, rounded physical corners, unclipped hover, cursor-centered animated pickup, free following, point-drop commitment and cancellation without lines. Actual renderer samples proved smooth135% enlargement for attack, defense, energy and enemy actions. Mouse/keyboard/touch placements auto-target correctly; energy-zero and cost-two-at-one-energy gating prevent targeting from starting, while free cards remain playable. Queue and attachment refunds update bubbles and re-enable cards; refund controls work inside disabled hand cards. At1280x800 the hand stayed above the energy meter and both fit the720px letterbox. Actual resolution produced guard42/Bob41 with hammer+vest+coffee and correctly refreshed three energy; no page errors in instrumented runs. Earlier attachment milestone verified card/position binding, expiry, turn4 victory and replay. CI runs build+Bun tests; browser smoke is manual. No multiplayer or multi-enemy encounter yet.
