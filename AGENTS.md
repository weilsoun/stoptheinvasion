# Stop the Invasion

## Product agreement
Single-player party deckbuilder, currently Bob versus alien-possessed store security in a replayable 1v1 combat laboratory. Campaign, map/shop/boss, items (potions), collectible statuses (relics), additional party characters and co-op are outside this milestone. The approved current combat model is one persistent, single-lane encounter timeline, not a six-slot queue or a two-lane packing board.

1920x1080 design coordinates; locked16:9 with letterboxing. The left60% is a card workspace: timeline/history above, hand below y720. The right40% is a diagonal combat stage: Bob nearer at its lower-left facing right, Possessed Security farther at its upper-right facing left. Existing upright character artwork is reused; staging and scale supply depth rather than new three-quarter poses. Comic-book horror comedy in MOREMART, a parody big-box store. Physical lit PlayCanvas cards have rounded corners, readable hover, animated pickup and action emphasis. Hand is fanned; timeline cards are straight, equally spaced and do not overlap. Mouse, keyboard and touch; no physics-driven combat.

Card artwork shows standalone actions or effects, not a specific matchup. The acting owner's hands, gear or identity may remain recognizable; recipients are neutral outlines, stick figures or schematic targets, never the recognizable opposing character. Keep thick organic comic ink, flat limited colors and empty backgrounds. Percussive Maintenance is a hand/claw-hammer impact on a stick figure; Warranty Voided uses a much larger sledgehammer; Steel-Toe Solution is a boot kicking a butt outline; Measure Once is a measuring/weak-point diagram. Trip Hazard uses a neutral stumbling dummy; Marked for Review uses an enemy-owned flashlight on a blank target. Preserve suitable standalone object/self illustrations, including Safety-ish Vest. All critical subjects must survive the centered1.25:1 card-art crop. `CardDefinition.art` may deliberately reuse a named preloaded generated comic illustration; never substitute geometric placeholder art. Bob's reusable front/side reference is `src/assets/characters/bob-reference.png`.

Card wording must stay recipient-generic: “Deal8damage,” not “Deal8damage to Bob.” Remove character names and specific-recipient phrases from rules, scaling templates, flavor and card-facing labels. Keep actor identities in HUD, explicit target controls and combat logs; targets still resolve through actor IDs. The user corrected “search cards” to SURGE energy cards; no draw-pile tutor/search implementation is retained.

## Alien possession and time theme
The invaders are time-manipulating aliens possessing ordinary people, not an infection turning them into monsters. Hosts retain their recognizable jobs, personalities and themed attacks: Possessed Security still checks receipts. Preserve the everyday caricatures and comic horror-comedy. The theme-only change updates naming, menu lore and encounter copy without changing artwork, stats, cards, timing or balance rules.

History is intended to become a resource through specific future abilities, not a freely editable extra hand. Reclaim (recover an actual card), Echo (temporarily replay a past action) and Rewind (restore an earlier combat state) remain possibilities. The user additionally proposed a CAD/Git-like Rewrite: edit a historical action, reapply the later recorded actions, and recompute the present. This is a design direction, not an implemented mechanic. Legal edits, costs, invalidated later actions, lethal branches and random/draw replay policy still need decisions. Current history remains read-only; do not implement a historical-edit mechanic without settling those rules.

## Persistent timeline rules
Positions are absolute, zero-based encounter coordinates. Higher indices are farther LEFT and later; lower indices are RIGHT and earlier. Resolution consumes INCREASING indices, visually RIGHT TO LEFT. Empty positions consume time. Enemy actions stay anchored to absolute positions regardless of turn boundaries, panning, zoom, reveal/hide or ordinary card placement. The schedule continues until victory or defeat; fifty cards is not a limit.

Planning is paused with unlimited time. Bob normally advances seven positions per turn. `turnLength(state)` adds active bracket-position modifiers to the actor's persistent base and clamps the result to at least one. `turnEnd(state)` is exclusive. Extending a turn immediately reveals and opens additional positions, including any enemy actions there. The following turn begins after the last consumed position and receives a fresh movement budget: extending0–6 to0–8 makes the next normal turn9–15. Short turns intentionally accelerate resource refresh along the track. Zero-distance resource-refresh loops are forbidden.

Only positions inside `[state.position, turnEnd(state))` accept planning commands. Scouting reveals a number of POSITIONS, not cards, beyond the bracket, up to exclusive `visibleEnd(state)`. Scouted future is inspect-only. Removing the scouting buff hides those positions again even if the schedule remains cached internally. Refunding an extension or scouting card after inspecting the revealed information is an intentional free planning tactic. Hidden positions must not leak their occupancy through meshes, DOM labels, hit targets or spacing. Revealed actions are reliable unless an explicit mechanic changes or cancels them.

`CardDefinition.bracket` cards attach to the current turn bracket, never to an ordinary timing position or actor. They reserve energy and immediately change length/scouting. Removing an extension or adding shortening atomically returns out-of-range player cards to hand and releases their commitments. Card-bound attachments follow returned player hosts. Position-bound attachments and attachments bound to an enemy that leaves the current bracket return to hand and refund, even if that enemy is still scouted. Temporary bracket attachments expire at cleanup; persistent actor turn-length/scouting stats remain. Unused attachments still cost energy when resolved.

History stores independent snapshots of every consumed position, including empty and canceled positions, original action/definition, applicable attachments, signed upgrade level and events. History is read-only and never part of inventory or energy accounting. Render-only history UIDs must differ from live card UIDs. Immediate lethal stops consumption at the lethal position; all remaining commitments expire/discard during terminal cleanup without firing.

## Ordinary combat and attachments
Ordinary cards automatically choose their sole valid living target: damage attacks the enemy, self-effects apply to Bob. More than one valid choice requires an explicit target; no living target rejects placement without mutation. No actor selectors for obvious targets or attachments. Insertion previews are pure. Inserting into an occupied player position shifts toward the nearest empty non-enemy position by absolute distance; ties favor the RIGHT/lower index. Occupied queued moves rotate intervening player cards while preserving gaps and enemy anchors. Invalid/full/outside drops never mutate state. Queue reordering and depth changes snap synchronously; no spring or bounce.

Affordability is stored energy plus temporary Surge minus all commitments. Unaffordable hand cards are opaque grayscale and cannot start play/targeting, but remain inspectable. Cost0 cards remain playable at0; refunds immediately re-enable affordable cards. Native attachment refunds remain usable inside disabled cards, and an unaffordable scalable card may receive an affordable upgrade. The meter has eight fixed24px spendable-energy slots with a compact +N overflow count above8; tooltip/aria text distinguish stored, Surge and committed amounts. This is not an energy cap. Ordinary resolution-generated energy cannot retroactively finance commitments; explicit planning-time Surge can fund new commitments immediately.

Upgrade/downgrade attachments take no timeline position. Duct Tape Upgrade gives1 level to a compatible friendly card; Trip Hazard gives-1 level to a compatible enemy card. Signed levels stack without a gameplay cap and expire this turn. Costs and action identity never change. Each eligible definition has authored `scaling` increments for its own effects or bracket fields, not a universal damage bonus or percentage. Damage, Block, Exposed, healing, energy, draw and scouting floor at zero. Extension/shortening magnitudes can weaken to zero but never reverse polarity. The turn itself still has a one-position minimum. Upgrade/downgrade source cards have no scaling and cannot receive modifiers; no recursive source chains.

Card-body targets bind exact UID and follow between hand, queue and attached bracket hosts. Position targets stay absolute and affect only compatible occupants. Only scalable living-owner cards are eligible; history/future remain untargetable. Active bracket upgrades immediately reconcile range/scouting and stranded commitments. Returning a bracket host to hand preserves its upgrades/reservations. Activating a Surge instead consumes its bound upgrades immediately with the source. Remaining attachments expire at cleanup, including unused hand/empty-position targets and terminal cleanup. Base definitions and enemy intents never mutate.

Hands-off resolution; dead actors never act. Block expires at the moving turn boundary. Exposed adds a flat bonus to the next incoming hit, then is consumed; stacks add. Healing caps at maximum HP without changing Block/Exposed. A hit on a pre-hit Exposed opponent is critical, using that Exposed bonus rather than another damage multiplier. Percussive Maintenance applies Ringing once per critical action. Ringing becomes active next turn, limits that actor to its first chronological action in that turn, then expires; repeated application refreshes rather than stacking duration. Bob cannot commit a second action while Ringing; attachments are not actions. Enemy actions canceled by Ringing retain their positions and are recorded in history.

Draw five, start stored energy two, gain two on subsequent turns, stored cap four; values live on actors. `Actor.surgeEnergy` is a separate temporary pool, not stored energy above cap. Each moving boundary discards/draws and refreshes stored energy after clearing all unused Surge. Unplayed cards discard unless retained; resolution-drawn cards survive to next planning. Timed played cards remain logically queued until cleanup while presentation excludes completed UIDs, preserving discard-before-draw identity. Immediate Surge activations are the explicit exception: they enter discard when played. Seeded reshuffling/replay are deterministic; no full outcome preview.

Surge cards (`CardDefinition.surge`) are immediate, non-refundable planning utilities and occupy no timeline position. `playSurge(state,uid):SurgeResult` pays the source and its bound grades by each card's owner BEFORE granting energy; existing grade reservations are not charged twice. All spending uses Surge before stored energy. The source and consumed grade instances enter discard exactly once, with independent energy/discard events returned for presentation. Cost0 Surge works at0 available; positive costs cannot borrow the energy being generated. Unused Surge expires at normal and terminal cleanup, never banks, and is not granted again during resolution. Ringing continues to limit timed queued actions, not planning utilities. Surge activations are currently logged planning events, not fabricated history positions; future Rewrite design must account for them explicitly.

The native Surge now detail action, or a drag to the left timeline/energy meter, activates Surge. Right-stage/outside/hand-only drops cancel. Capture the release hit BEFORE removing surge-drag styling because the energy meter is otherwise pointer-transparent. A short UI-only surging mode sequences discard flights and blocks overlapping play; opening the menu may cancel the animation without refunding an already-consumed card. Restart/teardown cancel pending waits and leave no stale flight.

## Architecture and ownership
- `src/game/types.ts`: shared contracts, Main owns changes.
- `src/game/content.ts`: card data and fixed absolute enemy schedule, Main owns tuning.
- `src/game/combat.ts`: deterministic combat; zero DOM/PlayCanvas imports.
- `src/view/types.ts`: scene/UI contract, Main owns changes.
- `src/view/scene.ts`, `src/view/art.ts`: physical PlayCanvas renderer and artwork.
- `src/ui.ts`, `src/style.css`: HUD, timeline camera, hit targets, planning interactions and playback.
- `scripts/balance.ts`, `scripts/balance-feedback.ts`: canonical seeded planner and diagnostic report.
- `src/main.ts`, `index.html`, package/config files: integration.

Combat API: `createCombat(seed?: number): CombatState`; `availableEnergy(state, actor?: ActorId): number`; `turnLength(state): number`; `turnEnd(state): number`; `visibleEnd(state): number`; `queueCard(state, uid, target: ActorId | null, slot: number): CommandResult`; `previewPlacement(state, uid, target, slot): QueueSlot[] | null`; `removeCard(state, slot)`; `moveCard(state, from, to)`; `retargetCard(state, slot, target)`; `resolveTurn(state): ResolutionStep[]`. Every slot argument and event `slot` is ABSOLUTE, not local to a turn. `state.position` is the current turn's absolute start. `queue` is an absolute-index cache with consumed entries nulled at cleanup; future cached intents remain unchanged. `activeSlot` is the absolute consumed position or null at cleanup. Do not rebase indices or derive timing from array length.

Attachment API: `canAttachModifier(state, uid, target): boolean`; `attachModifier(state, uid, target)`; `removeModifier(state, uid)`; `upgradeLevel(state, uid, slot: number | null): number`. The old damageModifier API is removed. Targets are `{kind:'card', uid}`, `{kind:'slot', slot}` or `{kind:'bracket'}`. Level-source (`modifier.levels`) and temporal bracket attachments are mutually exclusive. Mutating planning commands return CommandResult and mutate only on success. Preview is pure, shallow and accepts hand/queued UIDs while preserving targets. Reject invalid/dead targets, noninteger/out-of-bracket indices, wrong attachment targets and commands outside planning. Resolve preflights commitments before spending, never mutates its input, and returns independent snapshots for consumed positions plus cleanup. Nested history/actions/definitions/scaling/events/cards/attachments are independently cloned. Planning-only mutations copy affected arrays, not unused history.

`src/game/upgrades.ts` owns pure `applyUpgrade(base, level)` for CardDefinition/EnemyAction and `scaledBracket(base, level)` for frequent range queries without description allocation. Scaling description templates use `{effect:0}`, `{effect:1}`, `{positions}` (magnitude) and `{scouting}`; literal numbers such as Minimum1 remain unchanged. Missing increments leave a stat unchanged. Grade0 preserves the base description. Effective views may share unchanged base structures and must not be mutated by consumers; public resolution/history snapshots clone them independently. Grade lookup must not call turnEnd, because turnLength grades active bracket sources. Both TimelineEntry and CardVisual store `upgradeLevel`; definition/action stays the base snapshot. Apply effective effects once, never add a second flat bonus.

Draw/discard CombatEvents carry independent `cards` snapshots. Cleanup emits discard BEFORE draw, even when the same UID is discarded and immediately redrawn. Presentation consumes these events, not final-pile membership. Logs are bounded; history persists for the encounter.

## Presentation contract
Renderer API: `createScene(canvas): ScenePort`, contract in `src/view/types.ts`. CardVisual uses top-left design x/y, dimensions and rotation. Physical card faces/text/art are meshes, not DOM replicas. `getCardPose(uid)` returns actual displayed bounds/rotation/flip. Resting queued cards snap x/y, dimensions and roll in setCards, resetting spring velocities. Depth changes are immediate. Hand pickup and airborne actions retain springs/eased dimensions; reduced motion snaps poses. Hand fan2.3 degrees per offset; hover enlarges while retaining fan angle and matching rotated hit regions. Held/detail cards straighten. Timeline camera motion is separate from queue-order pose snapping.

Timeline nominal card150x210 with16px gaps. Exactly three zoom levels:65%,100%,125%. Zoom changes presentation only, not timing, visibility or capacity. Background drag moves the track with the pointer. Left/Right arrows browse future/history; Home returns to the active turn. Minus/plus change zoom and zero resets it. View controls are native accessible buttons. End Turn recenters on the execution point, preserves zoom and animates the track sliding RIGHT for every consumed position, including empties. The next planning phase frames its new bracket. Restart clears camera/history/position but preserves chosen zoom. Hand, piles, actor HUD and Resolve stay fixed. Cull offscreen card meshes and hit regions. The turn bracket and fog boundary supply visual structure; no persistent per-position numbers, instructional labels or empty-position diamonds. Keyboard focus may outline the selected destination.

Timeline navigation settles on whole-card centers. Pointer panning snaps on release/cancel; horizontal wheel bursts stay free while scrolling and snap after140ms idle. Native left/right buttons and keyboard arrows slide exactly one timing position, including empty positions, with180ms ease-out. Rapid clicks accumulate against the requested destination, not the intermediate animated pose. At the beginning, history navigation stops at position0; future browsing stays unbounded without revealing fog. Return/Home centers a middle card, including for even-length turns. Reduced motion snaps immediately. Navigation refreshes only the timeline subtree/card visuals to retain toolbar focus and rapid-click targets. Gestures, zoom, modals, playback, restart and teardown cancel pending navigation/snap timers.

The read-only timeline minimap is atx24/y226, width1104/height20, above the turn marker. Its viewport rectangle follows the actual visible card span, including the half-slot offset; current-turn band and occupancy ticks show history/current/scouted positions only. Unknown context is uniform and cached hidden intents never contribute ticks. Positions increase toward the LEFT. Pixel-bin aggregation bounds occupancy DOM nodes to256. The overview repaints with panning, snapping, zoom, turn progression and reset, and never captures pointer input. Virtual bracket-card anchors usey-65 so visible cards end aroundy222, above the overview.

Standard gamepad navigation is owned by src/gamepad.ts. Hold Select/Back8 + L1(4)/R1(5) for zoomout/in; Select + DpadLeft14/Right15 steps future/history. Start9 toggles the menu and is edge-triggered. Navigation repeat begins after350ms, then every150ms; opposing directions neutralize. Modal/surging/playback/drag guards, blur/disconnect rearming and teardown prevent stale input. UI supplies existing navigation/zoom/menu callbacks; no full controller hand-play mapping is implied. HTTPS or localhost and standard mapping are required. A virtual standard-gamepad fixture and reducer tests verified bindings; physical hardware was unavailable.

The user now wants subtle visible card slots integrated with turn markers. `.slot-guide` uses the same per-position pose/dimensions as the physical card, with faint rounded borders and empty-only tint/connectors to the timeline rail. Current slots use muted amber, scouted future slots quieter dashed borders, history a faded outline. Only known positions get guides; hidden future occupancy stays concealed. Guides are decorative, aria-hidden and pointer-transparent. The current-turn bracket has thin extended side rails; its hit area stays compact so blank-track panning still works. Active/drop/keyboard-focus emphasis uses the same card-sized guide. Detail mode hides the decorative HUD with the existing main visibility rule.

Split geometry is shared through `CARD_WORKSPACE`, `HAND_TOP`, `ACTOR_CENTERS` and `ACTOR_HUD` in view/types. Workspace is x0–1152; timeline hit targets/fog are clipped to its upper720px; hand/piles/energy/Resolve stay in its lower region. Hand and timeline center atx576; navigation controls are right-aligned atx1136/y56, clear of stacked bracket-card headers. `CardVisual.clip` supplies a design-space rectangle for resting faces, backs and badges. Renderer applies per-mesh shader clipping; no shared-material leakage. Dragged/detail/airborne cards and their attachments explicitly clear clipping, so flights cross into the fight stage. Right-stage drops cannot queue or refund. Scene-owned dividers stay behind cards, not over flights/details.

Enemy bodies are black with contrasting red title bands, charcoal rules panels and light outlines separating sections; artwork keeps its colors. Titles44px bold Arial; rules34px Arial with same-size extra-bold numerals; wrapped text vertically centered. Conditional critical text is separate below the primary action. Player fronts blend character color into rarity: basic gray, common white, uncommon green, rare gold, epic purple, legendary tie-dye rainbow. Current unranked cards use common. Builder identity defaults to orange, with an abstract builder back and orange front border.

Energy badges are round, square-face meshes under each card root, diameter18% of displayed width, centered4.5% inward from its top-left corner on both axes. They follow size, rotation, flip and dimming. Alpha-aware512x768 textures have rounded corners. DPR cap1.5. Actor/impact centers are Bob(1370,730), guard(1720,350), with no old visual-child lowering. Bob artwork scale1.05, guard0.75. HUD panels are Bob(1195,930) and guard(1545,100), both350px wide. Hand hover280x392 at y616, nominal bottom1008 before rotation; its horizontal position stays inside the workspace. Detail480x672 at(720,180) remains centered over the entire stage, unclipped and above the scene scrim.

Attachments render complete cards at90% of host width/height, tucked behind with a28px title peek scaled to host height. Hand hit regions match the90% width. Bracket hosts also display nested upgrade cards with independent grabbable headers. Live host details expose native inspect/refund buttons for attached modifiers, making tiny bracket headers optional for touch. A selected modifier tapping a bracket host targets it; it must not start dragging that host and clear selection. Upgrades highlight only numerals that actually changed in green; downgrades use red. Preserve original sentences, fonts, sizes and panels, including unchanged literal numbers; no replacement damage/level banner. Back planes use local Euler(90,180,0). Historical player cards retain player artwork and frozen grades, never enemy styling or live refund controls.

UI API: `mountGame(root, scene): GamePort`. Held cards ease to cursor center; release near a legal position snaps placement. Drag queued cards back to hand or use Delete/Backspace to refund while preserving bound attachments. Pull attachments off to refund; pointercancel preserves them. Drag-click handling must not swallow the next real Resolve/refund click. Card clicks inspect, including unaffordable/history/scouted cards. Close, Escape and outside clicks/taps dismiss and restore focus; interior clicks do not dismiss. History/future details expose no play/move/refund path. Hidden future details/hit targets must disappear when scouting retracts.

Horizontal trackpad/wheel input over the left timeline workspace pans the existing camera during planning. The stage owns a non-passive wheel listener on the shared AbortController so canvas and HUD targets both work and teardown removes it. Respect CSS-to-design scaling and pixel/line/page delta modes; positive horizontal wheel delta moves card content left. Ignore predominantly vertical input, Ctrl/Meta pinch/browser-zoom gestures, the hand/right combat stage, modals, and active drags/playback. Root html/body use `overscroll-behavior-x:none` to suppress browser history swipe without changing vertical overscroll. Native dialog scrolling remains available. Chromium wheel/default-prevention, scaling, modal scrolling and teardown were exercised; a physical Edge trackpad gesture was not available in the test environment.

Playback: lift150ms, travel360ms to the actual target, adopt effects at impact, hold130ms, then discard the player card or eject the enemy before the next action. Enemy intents never enter Bob's inventory. Animate draw/discard pile movement with flips; a recycled UID visibly discards first, snaps to draw origin, then deals back. Use render-only history UIDs and hide the new history copy until flight finishes. Outcomes appear after playback. Every wait checks sequence/destroy guards; restart/teardown cancel timers and resolve pending waits. Compact Menu contains Restart; Escape opens it when no interaction/modal needs canceling.

## Agent workflow
Main (Astra) owns planning, contracts/content, integration and verification. Implementation agents use openai-codex/gpt-5.6-sol. Work only assigned files; ask Main before shared-contract changes. Escalate material product choices through interactive Grill Me; use conservative implementation defaults. Skip builds/tests/formatters during concurrent edits; Main validates integrated changes. No nested delegation without independent ownership. Handoff: changed files, contract issues, verification not run. The primary timeline branch does not automatically modify the separate glossary or pile-grid worktrees/PRs.

Glossary status: `feat/term-glossary` in the separate `../stoptheinvasion-terms` worktree contains the keyword dictionary and contextual help prototype; preview port5174. It is not integrated into this primary build, and that worktree also contains uncommitted follow-up changes. Its timing and attachment definitions still describe six positions and damage-only modifiers. Port/update the dictionary and interactions for persistent turns, scouting/history and upgrade levels before integration; do not merge the old UI wholesale or discard the worktree's changes.

## Skills and commands
Read build-app and its direct-engine reference for bootstrap; apply-conventions for Engine changes; scene/light/HUD/state skills as appropriate. Official PlayCanvas skills are from https://github.com/playcanvas/skills (MIT, adjacent license). Interview skills are from https://github.com/mattpocock/skills. Use installed Engine types, official docs and browser verification; no MCP is needed.

`npm install`, `npm run dev`, `npm run build`, `bun test`, `npm run balance`. Preview: http://localhost:5173 or http://100.108.173.2:5173 over Tailscale. Browser smoke covers extension/retraction, scouting reveal/hide, future/history rejection, panning direction, all three zooms, recenter/playback, frozen history, keyboard/native buttons, touch cancellation, refunds, same-UID pile transitions, lethal/replay and letterboxing. Runtime state is authoritative. Do not assert screenshots or source text.

## Balance gate and content
Every new card and every change to costs, effects, deck, enemy schedule, resources or timing requires a fresh canonical balance run. Art-only changes do not. `npm run balance` uses32 paired seeds per candidate/policy, writes `.balance/report.json`, and reports outcomes, opportunity-normalized use, role gaps, measured tuning candidates and uncertainty. CLI: `--seeds N --output PATH`. Fingerprint includes game rules/data/types, analysis source and the full24-position enemy cycle; prior fixed-six reports do not validate this model. Coverage fails if a catalog card is never actually played/attached. Enemy actions need behavioral proof too. CI uploads the report even on coverage failure.

Planner enumerates temporal attachments, then grades on those hosts BEFORE ordinary placements, then remaining compatible grades. It plans only the current absolute bracket and never reads hidden cached intents. Analysis projects presentation-only history to[]; playable-position opportunities exclude enemy slots. Greedy evaluates effective authored damage, never treats levels as flat damage. Coverage records actual grade target effects/bracket fields, including nondamage player and enemy targets. Scouting has no fabricated information score. Strong/tactical/greedy remain bounded one-turn heuristics, not human/optimal-play evidence. Recommendations must cite measured candidates; no automatic production tuning. Fingerprints include upgrades.ts. Expanded stat-scaling studies retain32 paired seeds and have a30-minute CI deadline rather than dropping candidates or coverage.

Deck21 across13 definitions, with two copies each of Duct Tape Upgrade, Trip Hazard and Second Wind. Opening five are hammer, vest, overtime, lookout and surge; Coffee remains later in the deck. Second Wind costs0, grants2 temporary Surge immediately, and gains2 more per upgrade level. Each upgrade/downgrade still costs1. Other authored increments remain hammer/heavy damage4, vest Block3, tape Exposed2, Coffee energy1, toolbox draw1, brace damage2/Block2, overtime extension1, lookout scouting1, clockout shortening1; enemy damage4/Exposed1/healing2. Base enemy schedule and other card values are unchanged.

Current proof: strict TypeScript/Vite build and33 regressions/292 assertions across combat and controller input. Browser checks verified immediate Surge funding at0 spendable energy, source disposal without a timing slot, Surge-first resolution spending and expiry, generic rule text, and minimap movement/zoom/reveal-hide behavior. A browser-only reordered hand with storedenergy4 verified two Surge activations and two consumed upgrades yielding10 spendable energy, eight filled pips and +2 overflow. Virtual gamepad input verified Select chords, held-zoom edge behavior, Dpad repeat, Start menu edges, modal/focus guards and teardown. No physical-controller claim or permanent fixture data is made.

Current Surge balance study:32 paired seeds across76 runs/7296 fights and10,840,161 evaluated plans. Fingerprint `3c132ceeb11a130c57691926a50fd9c4b4a13c2f044ed38044b8f59b441bea7d`; no uncovered cards or authored scaling targets. Baseline strong/tactical/greedy won32/32,31/32,32/32; mean remaining HP22.90625/19.0625/10.125. Study-wide12836 successful immediate activations granted25528 temporary energy. Plans record real activation/group costs and consumed grade targets; Surge choices precede other commitments so the added funding can be used. These remain bounded heuristic results, not human/optimal-play evidence. No candidate tuning was applied automatically.

Split-layout proof: build and18 regressions/156 assertions pass. Chromium verified partial card clipping atx1152, matching face/back/badge clip uniforms, calibrated actor roots and HUD containment, right-stage drop rejection versus left-hand refunds, unclipped flights crossing the divider and reaching the guard, and full-stage details. Touch1280x800 verified letterboxing, native bracket attachment and cancellation. No game rules/data changed in this layout trial; numerical balance was not rerun.
